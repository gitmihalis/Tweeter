	<%= form_tag({:controller => 'favorites', :action => 'create' },
								:class => 'save_favorite_form' ) do %>
		<%= hidden_field_tag 'micropost_id', micropost.id %>	
		<%= submit_tag "Favorite" %>
	<% end %>